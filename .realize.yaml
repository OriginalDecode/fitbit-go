settings:
  legacy:
    force: false
    interval: 0s
schema:
- name: fitbit-fetcher
  path: .
  commands: 
    install:
      status: true
    build:
      status: true
    run:
      status: true
  watcher:
    extensions:
    - go
    paths:
    - /
    ignored_paths:
    - .git
    - .realize
    - vendor
